---
"@digilac/simap-mcp": patch
---

Changesets are now opt-in: contributors add one only when their PR produces a user-visible change. The CI changeset gate is removed; the [changeset-bot](https://github.com/apps/changeset-bot) still comments on every PR with the current status. Dependabot PRs no longer need an admin override to merge.
